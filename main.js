/**
 * Hello challenger! Your task is to write a function named `isPalindrome` that takes a string as input and returns true if the string is a palindrome and false otherwise.
 *
 * A palindrome is a word, phrase, number, or other sequence of characters that reads the same forward and backward (ignoring spaces, punctuation, and capitalization).
 *
 * - Level 1: Return `true` if the string is **not** a palindrome, otherwise `false`.
 * - Level 2: Return `true` if the string is a simple palindrome, otherwise `false`.
 * - Level 3: Return `true` if the string is a palindrome and has more than 12 characters, otherwise `false`.
 * - Level 4: Return 'true' if the string is a date which is palindromic number, otherwise 'false'.
 * - Level 5: Return 'true' if the string is a palindrome containing both letters and numbers, otherwise 'false'.
 *
 */
export function isPalindrome(str, level) {
    // Your code goes here! Look at the main.test.js file for examples.
  }


